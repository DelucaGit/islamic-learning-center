# Copy to frontend/.env.local (gitignored) and adjust if your Spring port differs.
# Next.js loads this when running `npm run dev` so next.config.ts rewrites hit the right origin.
BACKEND_ORIGIN=http://localhost:8081
